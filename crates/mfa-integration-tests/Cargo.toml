[package]
name = "mfa-integration-tests"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
mfa-archive = { path = "../mfa-archive" }
mfa-config = { path = "../mfa-config" }
mfa-contracts = { path = "../mfa-contracts" }
mfa-db = { path = "../mfa-db" }
mfa-ingestion = { path = "../mfa-ingestion" }
mfa-module-host = { path = "../mfa-module-host" }
tempfile.workspace = true
tokio.workspace = true
uuid.workspace = true
